export interface ValueCard {
  icon: React.ElementType;
  title: string;
  description: string;
}

export interface AdvantageCard {
  icon: React.ElementType;
  title: string;
  description: string;
}
